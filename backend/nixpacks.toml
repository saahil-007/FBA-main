[phases.setup]
nixPkgs = ["...", "libglvnd", "glib"]

[phases.install]
cmds = ["python -m venv venv", ". venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = "gunicorn -w 4 -k uvicorn.workers.UvicornWorker main:app --bind 0.0.0.0:$PORT"
